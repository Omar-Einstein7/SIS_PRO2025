// Keep this translation unit minimal — DataManager is implemented as managed code in DataManager.h.
// Remove native DM_* implementations to avoid conflicts with the managed DataManager class.
#include "DataManager.h"